---
name: jtennant-agent-config
description: How to update John's shared agent config repo. Use when the user asks to update, change, add to, publish, install, or explain jtennant-agent-config, agent config, shared Claude skills, or the installer.
---

# jtennant-agent-config

Repo:

```text
~/Development/jtennant-agent-config
```

First read the repo README:

```bash
cat ~/Development/jtennant-agent-config/README.md
```

Then make the requested minimal change, test if practical, and commit.
